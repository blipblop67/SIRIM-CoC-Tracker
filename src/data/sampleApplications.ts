import { SirimApplication } from '../types';

/**
 * Default initial applications list.
 * Starts empty so all data comes directly from real Gmail scans, Google Sheets, or user input.
 */
export const INITIAL_SIRIM_APPLICATIONS: SirimApplication[] = [];
